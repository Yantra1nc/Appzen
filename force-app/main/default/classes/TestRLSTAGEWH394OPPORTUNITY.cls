////////////////////////////////////////////////////
// This is an AutoGenerated class created by Rocketlane for its Salesforce integration
// DO NOT EDIT OR DELETE THIS CLASS
////////////////////////////////////////////////////



@isTest(SeeAllData=true)
public class TestRLSTAGEWH394OPPORTUNITY {

    static testMethod void test() {

        List<String> fields = new List<String>(Opportunity.SObjectType.getDescribe().fields.getMap().keySet());
        String soql = ''
                + ' select ' + String.join(fields, ',')
                + ' from Opportunity'
	            + ' order by createddate desc'
                + ' limit 1';
        List<SObject> records = Database.query(soql);

        Opportunity existingObj = (Opportunity)records.get(0);

	    test.startTest();
	    update existingObj;
        test.stopTest();
    }
}