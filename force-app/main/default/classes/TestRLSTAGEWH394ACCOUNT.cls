////////////////////////////////////////////////////
// This is an AutoGenerated class created by Rocketlane for its Salesforce integration
// DO NOT EDIT OR DELETE THIS CLASS
////////////////////////////////////////////////////



@isTest(SeeAllData=true)
public class TestRLSTAGEWH394ACCOUNT {

    static testMethod void test() {

        List<String> fields = new List<String>(Account.SObjectType.getDescribe().fields.getMap().keySet());
        String soql = ''
                + ' select ' + String.join(fields, ',')
                + ' from Account'
	            + ' order by createddate desc'
                + ' limit 1';
        List<SObject> records = Database.query(soql);

        Account existingObj = (Account)records.get(0);

	    test.startTest();
	    update existingObj;
        test.stopTest();
    }
}